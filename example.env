# Copy to .env (gitignored). ONLY needed for the OPTIONAL full-text harvest through your
# own institution's subscription access (src/browser_login.py and src/browser_*.py drive a
# Playwright browser through your library proxy). Every paper number reproduces from the
# bundled data/ with NO .env, NO key, and NO network.
#
# Institutional SSO credentials (yours; used locally by Playwright only):
KUL_USER=
KUL_PASS=
