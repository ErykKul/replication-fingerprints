# Reproduction package dependencies.
#
# Core analysis (Table 1, orthogonality, figures, composite: all embedder-free):
numpy>=1.24
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
matplotlib>=3.8
openpyxl>=3.1
#
# Novelty axis + dense-embedder ablation cells (MiniLM / all-mpnet via sentence-transformers):
sentence-transformers>=2.5
torch>=2.1
#
# Corpus REBUILD only (network; not needed to reproduce the numbers from the bundled data):
requests>=2.28
# playwright        # full-text harvest via institutional access (see README)
# PyMuPDF>=1.23     # pdf -> markdown conversion (src/convert.py)
